import styled from 'styled-components';

export const TextBox = styled.div`
    display: flex;
    justify-content: center;
    flex-wrap: wrap;
    align-items: start;
    margin-bottom: 25px;
    font-size: 1.2rem;
`;